# macOS System Files
.DS_Store
jarods-thoughts.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Secrets / credentials — NEVER commit. Fleet YAML and the spike carry provider
# names and model strings only; real keys/tokens live in Hermes auth or the env.
.env
.env.*
auth.json
*.auth.json
*.key
*.pem

# Local-only fleet variants (machine-specific values)
fleets/*.local.yaml

# Personal local scratch (checklists, notes) — never committed
*.local.md

# Local demo / run transcripts — capture live Hermes host, operator, and provider/credential state; never commit (Codex adversarial-review, 2026-06-18)
cadre-demo-*

# Planning and Agent Files
docs/brainstorms
docs/plans
docs/specs
# Origin research notes (early AI chats) — internal; the public reference is docs/reference/hermes/
docs/reference/[0-9]*-*.md
docs/reference/README.md
CLAUDE.md
.claude/
# Local Codex CLI config — machine-specific absolute PATH (points at this repo's
# .venv/bin); never commit to a public repo.
.codex/

